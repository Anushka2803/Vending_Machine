# Vending_Machine
A vending machine is implemented in Verilog using a finite state machine (FSM) to simulate the process of accepting coins and dispensing a product when the required amount is inserted. The system transitions between states based on the input coins and resets after delivering the item.
